# forgit

> A utility tool powered by fzf for using git interactively. https://github.com/wfxr/forgit

`ga: git add selector (ga)`
`glo: git log viewer (glo)`
`gi: .gitignore generator (gi)`
`gd: git diff viewer (gd)`
`grh: git reset HEAD <file> selector (grh)`
`gcf: git checkout <file> selector (gcf)`
`gcb: git checkout <branch> selector (gcb)`
`gbd: git branch -D <branch> selector (gbd)`
`gct: git checkout <tag> selector (gct)`
`gco: git checkout <commit> selector (gco)`
`grc: git revert <commit> selector (grc)`
`gss: git stash viewer (gss)`
`gsp: git stash push selector (gsp)`
`gclean: git clean selector (gclean)`
`gcp: git cherry-pick selector (gcp)`
`grb: git rebase -i selector (grb)`
`gbl: git blame selector (gbl)`
`gfu: git commit --fixup && git rebase -i --autosquash selector (gfu)`
